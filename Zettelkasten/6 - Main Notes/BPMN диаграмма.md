
15-11-2025 08:46

Status:

Tags:,[[Системный анализ]]

---
# BPMN диаграмма

Описывает логику~={cyan} бизнес-процесса=~.

~={red}!!!=~ не бизнес-логику приложения, а процесса ~={red}!!!=~


---
### Содержание

1. [[BPMN диаграмма#Уровни BPMN моделирования|Уровни BPMN моделирования]]
2. [[BPMN диаграмма#Элементы|Элементы]]
	- [[BPMN диаграмма#Задача|Задача]]
	- [[BPMN диаграмма#Подпроцесс|Подпроцесс]]
	- [[BPMN диаграмма#Циклы|Циклы]]
3. [[BPMN диаграмма#События|События]]
	- [[BPMN диаграмма#Промежуточные события (**граничные**)|Промежуточные события (граничные)]]
	- [[BPMN диаграмма#Операторы (шлюзы)|Операторы (шлюзы)]]
	- [[BPMN диаграмма#AND (Параллельный шлюз)|AND (Параллельный шлюз)]]
	- [[BPMN диаграмма#AND по событиям (Параллельный шлюз по событиям)|AND по событиям (Параллельный шлюз по событиям)]]
	- [[BPMN диаграмма#XOR (Исключающее ИЛИ) Эксклюзивный шлюз|XOR (Исключающее ИЛИ) Эксклюзивный шлюз]]
	- [[BPMN диаграмма#XOR зависимый от события (Эксклюзивный шлюз по событиям)|XOR зависимый от события (Эксклюзивный шлюз по событиям)]]
	- [[BPMN диаграмма#OR (Неэксклюзивный шлюз)|OR (Неэксклюзивный шлюз)]]
4. [[BPMN диаграмма#Потоки управления|Потоки управления]]	
	- [[BPMN диаграмма#Поток управления (Обычный)|Поток управления (Обычный)]]
	- [[BPMN диаграмма#Условный поток управления|Условный поток управления]]
	- [[BPMN диаграмма#Поток управления по умолчанию|Поток управления по умолчанию]]
	- [[BPMN диаграмма#Поток сообщений|Поток сообщений]]
	- [[BPMN диаграмма#Ассоциация|Ассоциация]]
5. [[BPMN диаграмма#Прочая хуйня|Прочая хуйня]]
6. [[BPMN диаграмма#Пример|Пример]]

---
### Уровни BPMN моделирования

- **Описательное моделирование**, когда нужно показать успешный путь выполнения бизнес процесса, например, чтобы согласовать его с бизнес-пользователем. Здесь применяются самые простые элементы нотации, а сама диаграмма намеренно максимально упрощается. 

- **Аналитическое моделирование** используется, когда нужно полностью показать все варианты выполнения бизнес-процесса, включая логические ветвления и альтернативы. Такая диаграмма обычно создаётся для опытных пользователей и бизнес-аналитиков с помощью расширенного алфавита нотации, включая не только её базовые самые простые элементы, но и более сложные.


---
## Элементы


#### Задача

![[Pasted image 20251115085414.png]]

Задача - простое действие, которое нельзя декомпозировать на подзадачи.

Задачи подразделяются на типы, каждый из которых (за исключением абстрактной задачи) обозначается своим маркером в левом верхнем углу блока задачи: 
- **Абстрактная задача** (задача с неопределенным типом); 
- Пользовательская задача (задача, которую выполняет человек при содействии других людей или программного обеспечения); 
- **Сервисная задача** (задача, предназначенная для оказания услуги, которая может являться как web-сервисом, так и автоматизированным приложением);
- **Отправка сообщений** (задача, суть которой заключается в отправлении сообщения внешнему участнику за пределы рассматриваемого процесса); 
- **Получение сообщений** (задача, суть которой заключается в получении сообщения от внешнего участника, находящегося за пределами рассматриваемого процесса); 
- **Ручное выполнение** (задача, выполнение которой подразумевает действия человека и исключает использование каких-либо автоматизированных механизмов исполнения или приложений); 
- **Бизнес-правило** (задача, суть которой заключается в выполнении бизнес-правила); 
- **Задача-сценарий** (задача, суть которой заключается в выполнении некоторого сценария (или скрипта) - некоторой автоматической операции).

> [!note] **Процесс-компенсация**
> Некоторые процессы могут приводить к нежелательным результатам, которые следует отменить. Процессы-компенсации как раз предусмотрены для отмены результатов выполнения некоторого процесса. Процессы компенсации не должны иметь входящих и исходящих потоков управления и могут соединяться входящей ассоциацией с граничным событием с типом "Компенсация". Пример соединения события и процесса с типом "Компенсация"
>
>![[Pasted image 20251115095525.png]]
>Для обработки компенсации используется [[BPMN диаграмма#Ассоциация|ассоциация]]

#### Подпроцесс

![[Pasted image 20251115085942.png]]

**Подпроцесс** - это декомпозированный процесс, включенный в состав рассматриваемого процесса, который описан более подробно на своей диаграмме. На диаграмме подпроцесс обозначается блоком со знаком "плюс" в центре нижней части фигуры.


Подпроцессы подразделяются на типы:
- **Подпроцесс** (подпроцесс с неопределенным типом); 

- **Событийный подпроцесс** (подпроцесс, не имеющий входящих и исходящих потоков управления. Событийный подпроцесс запускается всякий раз, когда его стартовое событие запускается во время выполнения родительского процесса); 

- **Транзакция**

- **Ad-Hoc процесс** (подпроцесс, представляющий собой группу процессов, взаимодействие между которыми не поддаются строго регламентированным правилам. Определяется только набор процессов, однако, их последовательность и количество выполнений определяются исполнителями этих процессов)

> [!note]
> Закрашенный конверт в левом верхенм углу - отправка сообщения этим действием.
> 
> Незакрашенный конверт - значит, что действие заблокировано до тех пор, пока не будет получено сообщение.

#### Циклы


- **Стандартный цикл** (используется, когда количество циклов заранее неизвестно. Процесс будет выполняться в цикле, пока верно некоторое условие); 

- **Многоэкземплярный параллельный цикл** (используется, когда количество циклов известно заранее. При этом экземпляры процесса будут выполняться параллельно); 

- **Многоэкземплярный последовательный цикл** (используется, когда количество циклов известно заранее. При этом экземпляры процесса будет выполняться последовательно).


----
#### События

- Событие 1 - стартовое событие с типом триггера "Сообщение"; 
- Событие 2 - промежуточное событие (обработчик) с типом триггера "Таймер"; 
- Событие 3 - промежуточное событие (инициатор) с типом триггера "Сигнал"; 
- Событие 4 - конечное событие с типом триггера "Сообщение".

![[Pasted image 20251115090426.png]]

**Событие** - состояние, которое является существенным для целей управления бизнесом и оказывает влияние или контролирует дальнейшее развитие одного или более бизнес-процессов.

Виды событий:

- **обработчики триггеров** (все стартовые и некоторые промежуточные события). Если встречается событие-обработчик, то процесс ожидает наступления этого события, т.е. ожидает появления причины возникновения этого события.

- **инициаторы триггеров** - События, которые инициируют триггер (или некий результат)


#### Промежуточные события (**граничные**)

Промежуточные события (обработчики) могут присоединяться к границе процесса. Такие события называются **граничными**. 

Граничное событие изображает событие, возникающее при выполнении процесса, к границе которого это событие присоединено. 

Виды граничных событий:
- **граничное прерывающее** - рерывает текущий процесс.
	![[Pasted image 20251115091152.png]]
	
- **граничное непрерывающее**. Граничное непрерывающее событие изображается пунктирными линиями.
	![[Pasted image 20251115091209.png]]
	


---
### Операторы (шлюзы)


#### AND (Параллельный шлюз)

![[Pasted image 20251115091342.png]]

Используется для:

- Ветвления потока:
	![[Pasted image 20251115091423.png]]
	
- Слияния потоков:
	![[Pasted image 20251115091452.png]]
	 Здесь выполнение Процесса 3 запустится только тогда, когда выполнится и Процесс 1, и Процесс 2.



#### AND по событиям (Параллельный шлюз по событиям)

![[Pasted image 20251115092807.png]]

Если выполняется Событие 1 -> выполняется Процесс 1. Если после этого возникнет Событие 2 -> выполнится Процесс 2 параллельно. 


#### XOR (Исключающее ИЛИ) Эксклюзивный шлюз


![[Pasted image 20251115091617.png]]

Используется для:

- ветвления потока:
	![[Pasted image 20251115091732.png]]
	При использовании эксклюзивного шлюза можно продолжить выполнение процесса только по одному из возможных условных потоков управления.
	
	Можно установить поток по умолчанию.
	
	Крч ~={cyan}как switch=~.

- слияния потоков:
	![[Pasted image 20251115091905.png]]
	 Выполнение продолжится при завершении 
	 ИЛИ Процесса 1 ИЛИ Процесса 2
	


#### XOR зависимый от события  (Эксклюзивный шлюз по событиям)

![[Pasted image 20251115092512.png]]

XOR ([[BPMN диаграмма#XOR (Исключающее ИЛИ) Эксклюзивный шлюз|Эксклюзтвный шлюз]]), но вместо условий - события.



#### OR (Неэксклюзивный шлюз)

Switch, но возможно выполнение нескольких вариантов одновременно.

Ветвление потока:
	![[Pasted image 20251115092217.png]]

Слияние:
	![[Pasted image 20251115092322.png]]
	Процесс 3 будет выполнен только тогда, когда выполнится и Процесс 1, и Процесс 2.


---
### Потоки управления


#### Поток управления (Обычный)

Является неконтролируемым, т.е. на поток не воздействуют никакие условия, и поток не проходит через шлюзы.

![[Pasted image 20251115094510.png]]

Отличие рисунка 18 от использования [[BPMN диаграмма#AND (Параллельный шлюз)|шлюза AND]] для слияния - без шлюза Процесс начнётся по завершении любого из предыдущих процессов.


#### Условный поток управления

![[Pasted image 20251115095021.png]]

Используется в [[BPMN диаграмма#OR (Неэксклюзивный шлюз)|OR]] и [[BPMN диаграмма#XOR (Исключающее ИЛИ) Эксклюзивный шлюз|XOR]]:

![[Pasted image 20251115094936.png]]


#### Поток управления по умолчанию

![[Pasted image 20251115095114.png]]

Также используется в [[BPMN диаграмма#OR (Неэксклюзивный шлюз)|OR]] и [[BPMN диаграмма#XOR (Исключающее ИЛИ) Эксклюзивный шлюз|XOR]].


#### Поток сообщений

![[Pasted image 20251115095158.png]]

Используется для отображения межпроцессного взаимодействия - для связи элементов потока со свернутыми пулами.

Показывает передачу сообщений или объектов из одного процесса в другой процесс или внешнюю ссылку.

![[Pasted image 20251115095427.png]]


#### Ассоциация

используется для отображения связи объектов данных и баз данных с процессами.

Передача объектов данных:

- Между не последовательными процессами:
	![[Pasted image 20251115095858.png]]
	
	
- Между последовательными процессами:
	![[Pasted image 20251115095937.png]]
	

> [!note] 
> Ассоциация также используется для указания обработчика компенсации:
> 
>![[Pasted image 20251115095525.png]]


---
### Прочая хуйня

![[Pasted image 20251115100335.png]]
![[Pasted image 20251115100237.png]]


---
### Пример:


На стр 23:

![[03_ОБА_ЛР2_Моделирование_бизнес_процессов_Нотация_BPMN (1).pdf]]


Ещё пример:

![[microservices_bpmn.png]]

`*` это пример bpmn диаграммы микросервисного приложения магазина, рассмотренного здесь: [[C4 diagram]].

----
#### [[BPMN диаграмма - Flashcards|Link to flashcards]]



---
### References:

Пиздатый моделер:
https://modeler.camunda.io/