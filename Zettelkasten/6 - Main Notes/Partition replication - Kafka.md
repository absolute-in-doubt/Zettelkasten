
07-02-2026 10:33

Status:

Tags: [[Kafka]] [[Java+]]

---
# Partition replication - Kafka

#### Partition Replication

Partitions  могут быть реплицированы, один из реплик становится лидером (**leader**), остальные — **followers** или **In-Sync Replicas**. Это обеспечивает отказоустойчивость и доступность.



### Follower

![[Pasted image 20260207103313.png]]

> [!warning] 
> Follower partitions могут отставть от lead partitions на пару милисекунд. Причина: они сами опрашивают lead partition, и он, соответственно не ждёт follower partitions перед тем как добавить новое сообщение.

**Follower** - зачаток будущего лидера. Может катастрофически отставать от **lead partition**. 


----
### In-Sync Replicas

![[Pasted image 20260207103845.png]]

**~={orange}После падения лидера один из ISR становится новым лидером, а один из followers - новым ISR.=~**


---
### Схема взаимодействия с partitions

![[Pasted image 20260201102019.png]]

Описание схемы (схема демонстрирует базовый принцип устойчивости Kafka: запись — только через лидера, чтение — обычно из лидера, а синхронизация поддерживается через фолловеров):

- **Производитель (Producer)** всегда записывает сообщения в лидера раздела (partition).
- **Потребители (Consumers)** из группы потребителей (Consumer Group) обычно читают данные с лидера, чтобы получать подтверждённые сообщения.

- **Фолловеры (Followers)** автоматически реплицируют данные с лидера, чтобы поддерживать копии лога в актуальном состоянии.
- **~={cyan}ISR (In-Sync Replicas)=~** — это набор брокеров, чьи копии данных синхронизированы с лидером (в примере `ISR = [101, 102, 103]`).

> [!note] **Для чего нужны Follower partitions**
> **Follower partitions нужны в первую очередь для отказоустойчивости и высокой доступности данных, а не для чтения или распределения нагрузки по consumer groups.** Они обеспечивают репликацию данных: если leader partition (единственный брокер, принимающий все чтение/запись для этой partition) упадёт, один из **ISR** автоматически становится новым leader без потери данных.

![[kafka_partitioning.jpg]]

Т.е. Consumer_1 читает с **leader partition_1** на Broker-1, который в свою очередь имеет копию - follower partition_1 на сервере Broker-2.

Consumer_2 читает тоже **исключительно из leader** partitions, но из разных, т.к. при настройке не указали, из какой конкретно читать. -> **~={red}порядок приёма сообщений может нарушится.=~**



----
#### [[Partition replication - Kafka - Flashcards|Link to flashcards]]



---
### References:

