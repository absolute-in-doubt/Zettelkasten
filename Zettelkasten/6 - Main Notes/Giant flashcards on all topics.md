
Index: [[Java+]] 

FILE TAGS: java_interview

Q: Что такое ООП? Перечисли принципы ООП.
A: Это методология программирования, в которой программа рассматривается как совокупность объектов.
- Объекты являются экземплярами классов;
- Классы образуют иерархию (иначе это абстрактное программирование)
	
Принципы ООП:
1. Инкапсуляция
2. Наследование 
3. Полиморфизм
4. Абстракция
5. Обмен сообщениями
6. Переиспользование
<!--ID: 1768999569861-->


Q: Что такое инкапсуляция?
A: Инкапсуляция - свойство системы, позволяющее объединить данные и методы работающие с ними, в класс и скрыть детали реализации от пользователя, открыв только то, что необходимо при последующем использовании.
	
Цель инкапсуляции — уйти от зависимости внешнего интерфейса класса (то, что могут использовать другие классы) от реализации. Чтобы малейшее изменение в классе не влекло за собой изменение внешнего поведения класса.
<!--ID: 1768999569870-->


Q: Что такое наследование?
A: Наследование – это свойство системы, позволяющее описать новый класс на основе уже существующего с частично или полностью заимствующейся функциональностью.
<!--ID: 1768999569878-->


Q: Что такое полиморфизм?
A: Полиморфизм - свойство системы использовать объекты с одинаковым интерфейсом без информации о типе и внутренней структуре объекта.
	
Полиморфная переменная - это переменная, которая может принимать значения разных типов. (Generic, Object).
	
- ad hoc функция - функция, имеющая разные реализации, в зависимости от переданных аргументов.
- параметрическая (generic) функция - ведёт себя одинаково для различных типов аргументов (add в Collection)
<!--ID: 1768999569884-->


Q: Что такое абстракция?
A: Абстрагирование - способ выделить исключительно важные для нашего использования характеристики объекта. 
	
Абстракция - набор исключительно важных для данного use case характеристик объекта.
<!--ID: 1768999569890-->


Q: Что представляет собой обмен сообщениями?
A: Объекты взаимодействуют, посылая и получая сообщения. Сообщение — это запрос на выполнение действия, дополненный набором аргументов, которые могут понадобиться при выполнении действия. В ООП посылка сообщения (вызов метода) — это единственный путь передать управление объекту. Если объект должен «отвечать» на это сообщение, то у него должна иметься соответствующий данному сообщению метод. Так же объекты, используя свои методы, могут и сами посылать сообщения другим объектам.
<!--ID: 1768999569895-->


Q: За что отвечает JVM?
A:  
1. Загрузка байткода (ClassLoaders)
2. Верификация байткода (CLassLoaders)
3. Интерпретация байткода (Execution Engine)
4. Предоставление памяти (Data areas)
5. Предоставление JNI (Java Native Interface)
6. Очистка мусора (GCs)
<!--ID: 1768999569901-->


Q: Чем отличается JRE, JVM и JDK?
A:   
- JVM - интерпретатор Java кода
- JRE = JVM + стандартный набор библиотек
- JDK:
	- JRE 
	-  `javac` компилятор  
	- **`java` (Interpreter/Loader):** Executes the compiled bytecode.
	- **`jar` (Archiver):** Packages multiple files into a single JAR archive.
	- **`jdb` (Debugger):** Used for finding and fixing errors in code.
	- **`javadoc` (Documentation Generator):** Generates documentation from source code comments.
	-  **Other tools:** `javap` (disassembler), `jconsole` (monitoring), and `keytool` (security).
<!--ID: 1768999569907-->


Q: Как работает Serial GC?
A: Serial GC - generational GC. Он делит Heap на Young generation (Eden, S0, S1) и Old generation.
- Minor GC - собирает только Young generation. Это копирующий сборщик. Использует Stop the World паузу, сам помечает и сам перемещает объекты в следующий generation. Если объект пережил несоклько циклов очистки, он перемещается в Old generation.
- Full GC - чистит young + old. **Запускается при заполнении Old generation.** 
- В Old generation очистка происходи по принципу mark-sweep-compact: сначала живые объекты помечаются как живые, после чего копируются в начало Old generation, для более быстрого выделения памяти впоследствие.
<!--ID: 1768999569913-->


Q: Как работает G1 GC?
A: Gi GC - Garbage-First GC. Представляет собой региональный generational collector. Он разбивает Heap на регионы (1-32МБ). Каждый регион относится к своему поколению (они могут быть случайно разбросаны по памяти).
- Minor GC. Проводит очистку Young generation регионов. Во время Stop the World паузы сам помечает живые объекты. Выполняется периодически. В первую очередь очищает регионы, где больше всего мусора.
- Цикл Mixed GC: 
  (запускается, когда заполненность кучи превышает определённый порог)
		
	- **Concurrent Mark** (работает параллельно с приложением): конкурентно помечает живые объекты во всей куче, находя «грязные» регионы.​
		
	- **Mixed GC evacuation pauses** (STW): из выбранных регионов (обычно молодые + наиболее мусорные old) живые объекты копируются в другие регионы, тем самым регионы очищаются и одновременно происходит компакция.​
	
Во время всех STW пауз количество очищаемых регионов подбирается так, чтобы не превышать установленное время очистки.
	
Данный GC имеет хорошее соотношение пауза/throughput.
<!--ID: 1768999569919-->

Q: Чем отличаются методы `Thread.sleep()` и `Thread.yield()`?
A: Метод `yield()` служит причиной того, что поток переходит из состояния работающий (running) в состояние работоспособный (runnable), давая возможность другим потокам активизироваться. Но следующий выбранный для запуска поток может и не быть другим.
	
Метод `sleep()` вызывает засыпание текущего потока на заданное время, состояние изменяется с работающий (running) на ожидающий (waiting).
<!--ID: 1769021203528-->


Q: Как работает метод `Thread.join()`?
A: Когда поток вызывает `join()` для другого потока, текущий работающий поток будет ждать, пока другой поток, к которому он присоединяется, не будет завершён.
<!--ID: 1769021203542-->

Q: Можно ли переопределить static метод в Child классе? Какой метод будет вызван в данных случаях?
```java
class Parent {
	public static void foo(){
		System.out.println("Parent");
	}
}
	
class Child extends Parent{
	public static void foo(){
		System.out.println("Child");
	}
	
	public static void main(String[] args){
		Child ch1 = new Child();
		ch1.foo(); // что выведет?
		Parent ch2 = new Child();
		ch2.foo(); // что выведет?
	}
}
```
A: Переопределить static метод нельзя, т.к. он принадлежит классу. И поэтому для static методов выполняется static binding.
	
> BTW: вызывать static метод через объект - антипаттерн
	
```java
class Parent {
	public static void foo(){
		System.out.println("Parent");
	}
}
	
class Child extends Parent{
	public static void foo(){
		System.out.println("Child");
	}
	
	public static void main(String[] args){
		Child ch1 = new Child();
		ch1.foo(); // "Child"
		Parent ch2 = new Child();
		ch2.foo(); // "Parent", т.к. тип объекта при компиляции - Parent, а то, что в нем лежит Child можно определить только динамически
	}
}
```
<!--ID: 1769063406788-->

Q: Что такое fail-fast и fail-safe iterators?
A: fail-fast iterators - бросают `ConcurrentModificationException`, еслм после создания итератора коллекция изменяется (Ошибка выкидывается при вызова методов итератора после внешнего изменеия коллекции).
	
```java
//Например такое будет работать:
Iterator<String> iter = list.iterator();
while(iter.hasNext()){
	String str = iter.next();
	...
}
list.add("a");
	
//=============================
//а вот тут уже бросит ошибку:
String newEl = iter.next();
```
	
fail-safe iterators - итераторы, не кидающие ошибок при внешних модификациях коллекции (работают с копией).
	
Примеры (их итераторы) (коллекции из `java.util.concurrent):
- CopyOnArrayList
- ConcurrentHashMap
<!--ID: 1769063730949-->


Q: В каких случаях нужна подобная запись? Что она обозначает
```java
class ExecutorService extends Executor {
	<T> Future<T> execute(Callable<T> callable);
}
```
A: Данная запись обозначает, что передаваемый параметр может иметь generic другого типа. По сути это эквивалентно такой записи:
```java
class ExecutorService extends Executor {
	<X> Future<T> execute(Callable<X> callable);
}
```
<!--ID: 1769101643195-->


Q: Класс А содержит метод: private void get(){...}. Может ли он быть вызван из статических методов класса?

A: Модификатор `private` ограничивает доступ **на уровне класса**, а не объекта: любые методы этого же класса (включая `static`) могут вызывать его private‑методы.
	 
Единственное условие для вызова **нестатического** private‑метода из `static` — иметь экземпляр: создать `new MyClass()` или получить его из аргумента/поля и вызвать `obj.privateMethod()`.
<!--ID: 1769101691851-->

Q: Что такое Maven? Для чего он используется? (Перечисли его функции)
A: **Maven** — инструмент автоматизации сборки проектов (прежде всего Java), основанный на декларативном описании проекта в `pom.xml` (Project Object Model).
	
**Основные функции Maven:**
	
- **Управление зависимостями**: автоматическая загрузка библиотек из репозиториев (Maven Central), разрешение конфликтов версий, транзитивные зависимости.
    
- **Стандартизированная сборка**: жизненные циклы (`clean`, `compile`, `test`, `package`, `install`, `deploy`) с предопределёнными фазами.
    
- **Генерация артефактов**: создание JAR, WAR, EAR и других пакетов.
    
- **Запуск тестов**: интеграция с JUnit/TestNG, Surefire плагин.​
    
- **Плагины**: расширяемость (компиляция, анализ кода, документация Javadoc, отчёты).​
    
- **Многомодульные проекты**: иерархическая структура с parent/child модулями.​
    
- **Репозитории**: локальный кэш + remote (Maven Central, Nexus, Artifactory).​
    
- **Соглашение над конфигурацией**: стандартная структура `src/main/java`, `src/test/java`.
<!--ID: 1769102313351-->

Q: Что такое mutex (mutual exclusion)? Зачем он нужен?
A: mutex - взаимоисключающая блокировка, семафор с одним состоянием - механизм, гарантирующий потоку исключительный доступ к ресурсу.
	
Используется для предотвращения одновременного доступа к общему ресурсу. В каждый момент времени таким ресурсом может владеть только один поток. Простейший пример: `synchronized(obj) { … }`.
<!--ID: 1769150844088-->

Q: Что такое кооперативная многозадачность? Какую модель многозадачности использует Java?
A: Кооперативная многозадачность - модель многозадачности, в которой потоки добровольно передают управление друг другу и уступают процессорное время в удобный момент.
	
Недостатки:
- если один поток завис -> не передаст управление -> приложение зависнет.
- если нет удобного момента для передачи управления - другие потоки will be starving.
	
Преимущества:
- простота реализации.
	
	
---
В Java используется вытесняющая многозадачность - модель многозадачности, в которой задачи переключает диспетчер. Переключение происходит в произвольные моменты.
	
Недостатки:
- сложность реализации
	
Преимущества:
- зависшие потоки не стопят прилложение
<!--ID: 1769238604918-->


Q: Что такое аннотации? Как они работают?  
A: Аннотации — это специальный синтаксис (@), позволяющий встраивать метаданные в код. Они помогают компилятору выявлять ошибки, оптимизировать производительность и упрощать поддержку кода, а также используются для генерации кода.​​
	
Аннотации добавляют метаданные к классам, методам и переменным для хранения информации о программе на этапе компиляции или выполнения, такой как конфигурация, зависимости или инструкции для фреймворков.​​
<!--ID: 1769417403186-->


Q: Какой тип информации можно предоставить с помощью аннотаций?  
A: Аннотации предоставляют метаданные о классах, методах, полях; они компилируются в байт-код и доступны через рефлексию на runtime для конфигурации, валидации или генерации кода.
<!--ID: 1769417403195-->


Q: Что такое retention policy у аннотаций в Java? Какие они бывают?
A: Retention policy определяет то, как долго будет удерживаться аннотация@Retention: 
- **SOURCE** - только исходный код, 
- **CLASS** (по умолчанию) - сохраняется в байткоде, но удаляется,к огда класс загружается в память. Недоступна через рефлексию. 
- **RUNTIME** - доступно на выполнении через рефлексию.
	
```java
@Retention(RetentionPolicy.RUNTIME)
public @interface MyAnnotation {
    // Annotation elements
}
```
<!--ID: 1769417403202-->


Q: Что означает @Target в аннотациях?  
A: @Target указывает, где аннотация может применяться: ElementType.METHOD, TYPE, FIELD и т.д., ограничивая её использование.
	
```java
@Retention(RetentionPolicy.RUNTIME) // Specifies the annotation is available at runtime
@Target({ElementType.METHOD, ElementType.FIELD}) // Restricts usage to methods and fields
public @interface MyAnnotation {
    String value() default "default value";
}
```
<!--ID: 1769417403211-->

Q: Как происходит сортировка в коллекциях (TreeMap), если хранимый объект(ключ) не реализует Comparable?
A: В TreeMap сортировка ключей происходит либо по естественному порядку (natural ordering, через `Comparable.compareTo()`), либо по предоставленному `Comparator`. Если ключ не реализует `Comparable` **и** не передан `Comparator`, то при попытке вставить первый элемент (или несовместимый ключ) возникает `ClassCastException` во время выполнения.
<!--ID: 1769446158972-->

Q: Что произойдет, если в TreeSet попробовать вставить два элемента, для которых метод `compareTo()` возвратит 0? 
A: В `TreeSet` нельзя вставить два объекта, для которых `compareTo()` возвращает 0 — второй вызов `add()` вернёт `false`, и размер множества не увеличится.
<!--ID: 1769446652316-->



Q: Что произойдет, если в TreeSet попробовать вставить два элемента, для которых метод `equals()` возвратит true? 
A: Всё будет ок, если для них компаратор или `compareTo()` возвращает не 0. Метод `equals()` для ключей в TreeMap не используется.
<!--ID: 1769446652323-->

Q: Какие есть способы передачи параметров по HTTP?
A:  
1. В теле запроса
2. В пути
3. В query parameters `...url/url?key=val&key1=val1`
4. В headers
<!--ID: 1769504544665-->


Q: У каких типов http запросов есть тело?
A: **Есть тело у:**
	- HEAD
	- POST
	- PUT
	- PATCH
	
**Нет тела у:**
	- GET
	- 
<!--ID: 1769763230352-->






Q: Чем отличаются PATCH и PUT запросы?
A: Предназначением. PATCH нужен для передачи исключительно части объекта для обновления, а PUT - полная замена.
	
|Характеристика|PUT|PATCH|
|---|---|---|
|**Область обновления**|Полная замена ресурса|Частичное обновление|
|**Идемпотентность**|✅ Да|❌ Нет (по умолчанию)|
|**Тело запроса**|Все поля ресурса|Только изменяемые поля|
|**Отсутствующие поля**|Удаляются/обнуляются|Остаются без изменений|
	
(**Идемпотентность** — это свойство операции, при котором повторное выполнение **не меняет состояние** системы сверх первого раза. Один запрос = несколько запросов.)
<!--ID: 1769504544683-->


Q: Для чего нужны FactoryBeans в Spring? Как они работают? 
A: FactoryBean - интерфейс, который мы реализуем для того, чтобы задать катомную логику создания объектов определённого типа, например здесь - метод `getObject()` возвращает рандомный тип цвета:
	
```java
public class ColorFactory implements FactoryBean<Color> {
    @Override    
    public Color getObject() throws Exception {
		Random random = new Random();
		Color color = new Color(random.nextInt(255), random.nextInt(255), random.nextInt(255));        
		return color;    
	}
	
    @Override
	 public Class<?> getObjectType() {
		 return Color.class;    
	}    
	
	@Override    
	public boolean isSingleton() {        
		return false;    
	}
}
```
	
Для настройки через Java-config это бессмысленно, т.к. там мы и так можем внести кастомную логику. Это устаревший интерфейс для XML настройки.
<!--ID: 1769519090307-->

Q: Почему рекомендуется ставить @Autowired именно на конструкторе, а не на полях/сеттерах?
A:  Рекомендация ставить `@Autowired` на **конструктор** (или вообще убирать `@Autowired` с единственного конструктора) связана именно с **гарантией полного внедрения зависимостей** и **ранним обнаружением проблем**.
	
Если поставить его на поля/сеттеры, то в случае, если бин не был найден, объект создастся без проблем,но вместо нужных зависимостей у него будет null. Таким образом, мы гарантируем, что все зависимости будут внедрены.
<!--ID: 1769526777852-->


Q: Есть ли у метода .of() интерфейса Collection (List.of(...)) ограничение по количеству объектов?
A: Да, максимум - 10 аргументов.
```java
// ✅ До 10 аргументов — прямой вызов
List.of("a", "b", "c");           // OK
List.of("a", "b", "c", ..., "j"); // 10 элементов — OK
	
// ✅ Больше 10 — через Collection
List.of("a", "b", ..., +100500);  // Компиляция ОШИБКА!
List.copyOf(bigList);             // ✅ Любое количество!
```
<!--ID: 1769596656575-->

Q: Можно ли в Java сравнивать Enum через `==`?
A: Можно и нужно. **Enum — синглтоны**
```java
enum Day {
     MONDAY, 
     TUESDAY, 
     WEDNESDAY 
}
```
	
**Каждое значение Enum** — **единственный экземпляр** в JVM (class loading time):
	
```java
Day d1 = Day.MONDAY; 
Day d2 = Day.MONDAY; 
Day d3 = Day.valueOf("MONDAY"); 
	
System.out.println(d1 == d2);  // true — тот же объект! 
System.out.println(d1 == d3);  // true — тот же объект!
```
<!--ID: 1769612957435-->

Q: Что произойдёт, если на одном объекте `Thread` вызвать `start()` два раза?
A: Если метод start() вызывается дважды для одного и того же объекта Thread, во время выполнения генерируется исключение IllegalThreadStateException.
<!--ID: 1769666279940-->

Q: Если в synchronized блоке вызвать `Thread.sleep(...)`, освободит ли поток монитор на время сна?
A: Нет.
<!--ID: 1769670881803-->


Q: Может ли в одном Spring проекте быть несколько конфигураций? (Для одной сборки) Как их все подтянуть и загрузить вместе?
A: Yes, in large projects, having multiple Spring configurations is recommended to increase maintainability and modularity.
	
We can load multiple Java-based configuration files:
	
```java
@Configuration
@Import({MainConfig.class, SchedulerConfig.class})
public class AppConfig {
```
	
Or we can load one XML file that will contain all other configs:
	
```java
ApplicationContext context = new ClassPathXmlApplicationContext("spring-all.xml");
```
	
And inside this XML file we’ll have the following:
	
```xml
<import resource="main.xml"/>
<import resource="scheduler.xml"/>
```
<!--ID: 1769670881810-->

Q: В чем разница между IO и NIO?
A: Java IO (input-output) является потокоориентированным, а Java NIO (new/non-blocking io) – буфер-ориентированным. Потокоориентированный ввод/вывод подразумевает чтение/запись из потока/в поток одного или нескольких байт в единицу времени поочередно. Данная информация нигде не кэшируются. Таким образом, невозможно произвольно двигаться по потоку данных вперед или назад. В Java NIO данные сначала считываются в буфер, что дает больше гибкости при обработке данных.
	
- Потоки ввода/вывода в Java IO являются блокирующими. Это значит, что когда в потоке выполнения вызывается `read()` или `write()` метод любого класса из пакета `java.io.*`, происходит блокировка до тех пор, пока данные не будут считаны или записаны. Поток выполнения в данный момент не может делать ничего другого. Неблокирующий режим Java NIO позволяет запрашивать считанные данные из канала (channel) и получать только то, что доступно на данный момент, или вообще ничего, если доступных данных пока нет. Вместо того, чтобы оставаться заблокированным пока данные не станут доступными для считывания, поток выполнения может заняться чем-то другим. Тоже самое справедливо и для неблокирующего вывода. Поток выполнения может запросить запись в канал некоторых данных, но не дожидаться при этом пока они не будут полностью записаны.
	
- В Java NIO имеются селекторы, которые позволяют одному потоку выполнения мониторить несколько каналов ввода. Т.е. существует возможность зарегистрировать несколько каналов с селектором, а потом использовать один поток выполнения для обслуживания каналов, имеющих доступные для обработки данные, или для выбора каналов, готовых для записи.
<!--ID: 1769680174790-->


Q: Какие существуют виды потоков ввода/вывода?
A: Разделяют два вида потоков ввода/вывода:
	
- **байтовые** - `java.io.InputStream`, `java.io.OutputStream`;
- **символьные** - `java.io.Reader`, `java.io.Writer`.
<!--ID: 1769680174798-->

Q: Что произойдёт, если поставить @Autowired на обычный метод класса?
A: Когда Spring находит `@Autowired` на методе, Spring автоматически вызовет этот метод, после создания экземпляра бина. В качестве аргументов, будут подобраны подходящие объекты из контекста Spring.
<!--ID: 1769763230361-->


Q: В чем разница между Сквозной Функциональностью (Cross Cutting Concerns) и АОП (аспектно ориентированное программирование)?
A: Сквозная Функциональность — функциональность, которая может потребоваться вам на нескольких различных уровнях — логирование, управление производительностью, безопасность и т.д. АОП — один из подходов к реализации данной проблемы
<!--ID: 1769763230367-->


Q: Какие parent интерфейсы есть у Collection?
A: Iterable
<!--ID: 1769763230375-->


Q: Как вы решаете какой бин внедрить, если у вас несколько подходящих бинов? Расскажите о `@Primary` и `@Qualifier`.
A: Если есть бин, который вы предпочитаете большую часть времени по сравнению с другими, то используйте `@Primary`, и используйте `@Qualifier` для нестандартных сценариев.
	
Если все бины имеют одинаковый приоритет, мы всегда будем использовать `@Qualifier`
	
Если бин надо выбрать во время исполнения программы, то эти аннотации вам не подойдут. Вам надо в конфигурационном классе создать метод, пометить его аннотацией `@Bean`, и вернуть им требуемый бин.
	
| Сценарий                 | @Primary (на бине)                   | @Qualifier (при инъекции)                |
| ------------------------ | ------------------------------------ | ---------------------------------------- |
| **Автоматический выбор** | ✅ `PaymentService` → `PaypalService` | ❌ Требует явного указания                |
| **Явная инъекция**       | `@Autowired PaymentService ps`       | `@Qualifier("stripe") PaymentService ps` |
| **Приоритет**            | Низкий                               | **Высокий** (перекрывает @Primary)       |
| **Где ставить**          | На **бине**                          | При **инъекции**                         |
	
```java
@Service @Primary
class EmailNotification implements NotificationService { }
	
@Service
class SMSNotification implements NotificationService { }
	
@Service
class OrderService {
    // Автоматически EmailNotification (Primary)
    @Autowired
    NotificationService defaultNotification;  
    
    // Явно SMS (Qualifier перекрывает Primary)
    @Autowired @Qualifier("SMSNotification") 
    NotificationService smsNotification;
}
```
<!--ID: 1769763230384-->



Q: В чем разница между `@Controller` и `@RestController`?
A: `@RestController` = `@Controller` + `@ResponseBody`
	
`@RestController` превращает помеченный класс в Spring-бин. Этот бин для конвертации входящих/исходящих данных использует Jackson message converter. Как правило целевые данные представлены в json или xml.
<!--ID: 1769763230391-->


Q: Где должны располагаться статические (css, js, html) ресурсы в Spring MVC приложении?
A: Расположение статических ресурсов можно настроить. В документации Spring Boot рекомендуется использовать /static, или /public, или /resources, или /META-INF/resources.
<!--ID: 1769763230399-->


Q: Можно ли передать в запросе один и тот же параметр несколько раз?
A: Пример: `http://localhost:8080/login?name=Ranga&name=Ravi&name=Sathish` Да, можно принять все значения, используя массив в методе контроллера
	
```java
public String method(@RequestParam(value="name") String[] names){
}
```
<!--ID: 1769763230409-->

Q: Что делает аннотация @RequestBody в методе контроллера?
A; `@RequestBody` **триггерит десериализацию** тела HTTP-запроса в Java-объект с помощью **HttpMessageConverter** (по умолчанию **Jackson** для JSON). Spring читает `Content-Type` запроса и автоматически парсит тело.
	
```java
@PostMapping("/users")
public User create(@RequestBody User user) {  // JSON → User объект
    return userService.save(user);
}
```
	
- `application/json` → **Jackson**
    
- `application/xml` → **Jaxb2**
    
- `application/x-www-form-urlencoded` → **FormHttpMessageConverter**



//Q: Чем отличается процесс от потока?
//A: 

